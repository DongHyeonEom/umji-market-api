# 파일

## 구현 상태

파일 업로드 API와 object storage 연동은 아직 구현되지 않았습니다. 카탈로그 이미지는 현재 `storageKey` 메타데이터만 저장하며, 업로드 URL 발급·파일 접근 권한 관리는 별도 기능입니다.

파일 저장소를 도입할 때 공개 상품 이미지와 비공개 사업자 증빙의 접근 정책을 분리하고, 원본 파일은 DB에 저장하지 않습니다.
